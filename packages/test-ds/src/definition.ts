/**
 * Side-effect-free definition entry consumed by createSystem/createTheme
 * extension. Components remain on the package root so the extraction sandbox
 * evaluates only the two built configuration values here.
 */
export { ds as system } from './system';
export { referenceTokens as theme } from './theme';
