{
  "name": "@animus-ui/parity",
  "version": "0.0.1",
  "private": true,
  "description": "Committed v2 parity oracle with fresh-process determinism, seam, and consumer-surface comparison",
  "type": "module",
  "scripts": {
    "parity": "bun run src/cli.ts",
    "compile": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@animus-ui/extract": "workspace:*",
    "lightningcss": "^1.30.2",
    "oxc-parser": "^0.139.0"
  }
}
