import { MarkdownContent } from '../../components/docs/MarkdownContent';
import content from '../../content/concepts/responsive-props.md?raw';

export default function ResponsiveProps() {
  return <MarkdownContent source={content} />;
}
