// CJS-style target re-export. The reconciler's getExportsOfFile maps
// `export = X;` (TSExportAssignment) to the symbol `default`; a barrel
// re-exporting that default under a named binding is live and SHALL NOT
// be stripped.
export { default as X } from './cjs-target';
