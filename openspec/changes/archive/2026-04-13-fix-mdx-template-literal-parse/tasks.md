## 1. Fix

- [x] 1.1 Replace backtick inline code at `theming.mdx:358` with `<code>{'Record<name, `var(--${string})`>'}</code>`

## 2. Verify

- [x] 2.1 Build showcase (`bun run verify:showcase`) and confirm no runtime errors on theming page
