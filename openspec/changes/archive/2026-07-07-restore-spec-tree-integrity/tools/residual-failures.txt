Residual validation failures after mechanical canonicalization (inc 02, 2026-07-07)

spec/color-mode-palette — 2 requirements lack scenarios (ColorPalette is accessible from navigation; ColorPalette swatches have hover preview). Authoring scenarios = new test-contract content. → content-triage follow-up.
spec/responsive-shell-layout — 3 requirements lack scenarios (Content width is consistent across pages; Animations respect reduced-motion preference; Drawer implements keyboard focus management). → content-triage follow-up.
spec/content-migration — prose design doc, zero requirement blocks; predates spec discipline. Candidate for archive-out with Reason+Migration or rewrite as real requirements. → content-triage follow-up.

Hand-applied removals (recorded per D2 loss-recording, applied by journaled hand edits in inc 02):
- prop-system: 'Responsive array syntax' — inline `### REMOVED —` marker applied (Reason/Migration preserved in git history of the pre-change file); also recorded in inventory.json.

Triage worklist pointer for the follow-up change: machine-generated boilerplate Purpose sections are greppable via: rg -l '^Requirements for the `' openspec/specs/ (76 files at inc-02 landing). Each is a candidate for a human-written Purpose during content triage.

Authored-content disclosure (NS2 exception, accepted review objection): includes-driven-discovery 'Plugin parity' requirement had no scenario text at HEAD; its 'Discovery parity across bundlers' scenario is a WHEN/THEN restatement of the requirement sentence itself — authored during inc 02's dialect conversion, not converted from existing WHEN text.
