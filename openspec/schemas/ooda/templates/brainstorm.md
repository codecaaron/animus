<!--
brainstorm.md — the STRATEGIC exploration record for the whole change.

HISTORICAL RECORD: immutable once design.md exists; design.md supersedes this
file on any conflict. An append-only exploration log is valuable; a second
live copy of the decision state is not — never sync this file after design.md
lands.

ADMISSION TEST — complete when it contains (content, not format: bullets
suffice; polished tables belong to design.md):
  1. the KNOWN-NOW vs DEFERRED split — each deferred variable tagged with the
     concrete resolving signal (a built artifact, a measurement, a resolved
     dependency) that will let a future increment resolve it. Naming a
     variable deferred is a valid, first-class outcome — do not force
     premature decisions.
  2. candidate NORTH STAR criteria — directional invariants to steer by, with
     revisit signals where provisional (seeds design.md §North Star).
  3. candidate GUARDRAILS — negative invariants with a sketch of an
     executable check each (seeds design.md §Guardrail Register).
  4. the decision chain that produced 1-3 — the skill's natural decision-log
     output, or a citation of pre-existing exploration evidence (an audit, a
     spike, an approved proposal) placed at the TOP of this file.

Produced by superpowers:brainstorming when exploration hasn't happened yet
(the DEFAULT path — output redirected here, not docs/superpowers/), or
captured directly from existing exploration evidence, skill skipped. Preserve
the exploration's natural organization; do not force a template onto it.

design.md reorganizes this content into the decision/direction/constraint
record (Decisions, North Star, Decision Ledger, Guardrail Register); tasks.md
separately owns decomposition (Increment Registry). Do NOT copy this file's
content into either — they are complementary, not duplicates.
-->
